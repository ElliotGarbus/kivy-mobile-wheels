# Android wheel builds. SKELETON — Phase 2 fills in the build steps.
#
# Manual dispatch only for now: these builds are slow and their inputs change
# rarely, so there is nothing useful to trigger on push.
name: build-android

on:
  workflow_dispatch:
    inputs:
      target:
        description: Which recipe to build
        required: true
        type: choice
        options: [sdl2, kivy-2.3.1-sdl2, kivy-3.0-sdl3, pyjnius]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        abi: [arm64-v8a, x86_64]
    steps:
      - uses: actions/checkout@v4

      - name: Show pinned inputs
        run: |
          echo "Building ${{ inputs.target }} for ${{ matrix.abi }}"
          cat recipes/PINNED_REFS.toml

      # Kivy's setup.py shells out during the build, and cibuildwheel's
      # cross-build interpreter IS Android CPython, which looks for Android's
      # shell path on this Linux host. Without this the build dies with
      # FileNotFoundError: '/system/bin/sh'.
      - name: Provide /system/bin/sh
        run: sudo mkdir -p /system/bin && sudo ln -sf /bin/sh /system/bin/sh

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install NDK r27.3.13750724
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            "ndk;27.3.13750724" "cmake;3.22.1" >/dev/null
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/27.3.13750724" >> "$GITHUB_ENV"

      - name: Run recipe
        run: bash recipes/android/${{ inputs.target }}.sh ${{ matrix.abi }}

      # sdl2.sh writes the Java glue from the same verified tarball that built
      # the library. If that differs from what is committed, the two have
      # drifted — which on-device means a black screen with nothing in logcat,
      # so it is caught here instead.
      - name: SDL glue matches the committed copy
        if: inputs.target == 'sdl2'
        run: |
          if ! git diff --quiet -- recipes/android/sdl-glue; then
            echo "::error::sdl-glue differs from the pinned tarball extraction."
            git diff --stat -- recipes/android/sdl-glue
            exit 1
          fi
          echo "glue matches"

      # TODO(Phase 2): wheel recipes only — flat .libs/ with unchanged sonames,
      # every .so at p_align=0x4000. sdl2.sh already verifies its own output.
      - uses: actions/upload-artifact@v4
        if: inputs.target != 'sdl2'
        with:
          name: ${{ inputs.target }}-${{ matrix.abi }}
          path: .build/android/wheelhouse/*.whl
          if-no-files-found: error
